spring:
  output:
    ansi:
      enabled: always

  cloud:
    discovery:
      enabled: false

  jpa:
    show-sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

